trigger LiveChatTranscriptTrigger on LiveChatTranscript (after update) {
    // Trigger to link person account with case and chat transcript, when live agent chat is done.
  //  LiveChatTranscriptTriggerHelper.assignAccountToCase(trigger.new, trigger.oldMap);
}