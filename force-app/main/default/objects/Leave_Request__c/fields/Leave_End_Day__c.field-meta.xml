<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Leave_End_Day__c</fullName>
    <description>Shows the weekday as per the Leave End Date.</description>
    <externalId>false</externalId>
    <formula>IF(WEEKDAY(Leave_End_Date__c) == 1, &apos;Sunday&apos;, IF(WEEKDAY(Leave_End_Date__c) == 2, &apos;Monday&apos;, IF(WEEKDAY(Leave_End_Date__c) == 3, &apos;Tuesday&apos;, IF(WEEKDAY(Leave_End_Date__c) == 4, &apos;Wednesday&apos;, IF(WEEKDAY(Leave_End_Date__c) == 5, &apos;Thursday&apos;, IF(WEEKDAY(Leave_End_Date__c) == 6, &apos;Friday&apos;, IF(WEEKDAY(Leave_End_Date__c) == 7, &apos;Saturday&apos;, &apos;&apos;) ) ) ) ) ) )</formula>
    <formulaTreatBlanksAs>BlankAsZero</formulaTreatBlanksAs>
    <label>Leave End Day</label>
    <required>false</required>
    <trackHistory>false</trackHistory>
    <trackTrending>false</trackTrending>
    <type>Text</type>
    <unique>false</unique>
</CustomField>
